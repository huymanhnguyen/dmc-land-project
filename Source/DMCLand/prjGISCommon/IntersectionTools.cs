using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DMC.GIS.Common
{
    public  class IntersectionTools
    {
        ///// <summary>
        ///// Lấy phần giao của một Feature với lớp chứa nó
        ///// </summary>
        ///// <param name="map">Đối tượng bản đồ chứa lớp có đối tượng cần lấy phần giao</param>
        ///// <param name="feature">Feature cần lấy phần giao</param>
        ///// <param name="strLayerName">Tên lớp chứa Feature cần lấy phần giao</param>
        ///// <returns></returns>
        //public MapInfo.Data.Feature IntersectFeatureWithLayer(MapInfo.Mapping.Map map, MapInfo.Data.Feature feature, string strLayerName)
        //{
        //    /* Chắc chắn rằng tồn tại đối tượng bản đồ */
        //    if (map == null)
        //        return null;
        //    /* Chắc chắn rằng tồn tại lớp có tên là strLayerName trên đối tượng bản đồ */
        //    if (map.Layers[strLayerName] == null)
        //        return null;
        //    /* Chắc chắn rằng tồn tại Feature cần lấy phần giao của nó với lớp chứa nó */
        //    if (feature == null)
        //        return null;

        //    MapInfo.Geometry.FeatureGeometry g = new MapInfo.Geometry.MultiPolygon(coordsys, MapInfo.Geometry.CurveSegmentType.Linear, dPoints);
        //    MapInfo.Geometry.Ring
        //}



    }
}
